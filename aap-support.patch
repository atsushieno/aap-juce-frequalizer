diff --git a/CMakeLists.txt b/CMakeLists.txt
index ae85d2e..156b48a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,4 +1,4 @@
-cmake_minimum_required(VERSION 3.13.0)
+cmake_minimum_required(VERSION 3.15.0)
 project(frequalizer VERSION 1.1.0)
 add_subdirectory(External/JUCE)
 
@@ -18,6 +18,8 @@ if (APPLE)
     list(APPEND FORMATS "AU")
 endif()
 
+set(FORMATS "Standalone")
+
 # add the plugin targets
 juce_add_plugin(frequalizer
                 VERSION "1.1.0"
@@ -38,6 +40,38 @@ juce_add_plugin(frequalizer
 add_subdirectory(Resources)
 add_subdirectory(Source)
 
+# begin AAP specifics. -->
+
+# They are needed in desktop too, for generate-aap-metadata.
+include_directories(
+        "${AAP_DIR}/native/androidaudioplugin/core/include"
+        "${AAP_DIR}/native/plugin-api/include"
+        )
+juce_add_module(${AAP_JUCE_DIR}/modules/juceaap_audio_plugin_client)
+
+# begin Android specifics -->
+# It is not beautiful, but there does not seem to be any way to add custom include directory for modules.
+
+if (ANDROID)
+
+# dependencies
+find_library(log "log")
+find_library(android "android")
+find_library(glesv2 "GLESv2")
+find_library(egl "EGL")
+set(cpufeatures_lib "cpufeatures")
+set(oboe_lib "oboe")
+
+target_include_directories(frequalizer PRIVATE
+        "${ANDROID_NDK}/sources/android/cpufeatures"
+        "${OBOE_DIR}/include"
+        )
+
+endif (ANDROID)
+# <-- end Android specifics
+# <-- end AAP specifics
+
+
 # add required flags
 target_link_libraries(frequalizer PRIVATE juce::juce_recommended_warning_flags juce::juce_recommended_config_flags juce::juce_recommended_lto_flags)
 target_link_libraries(frequalizer PRIVATE juce::juce_opengl juce::juce_dsp juce::juce_audio_utils)
@@ -46,6 +80,28 @@ target_compile_definitions(frequalizer
     JUCE_VST3_CAN_REPLACE_VST2=0
     JUCE_WEB_BROWSER=0)
 
+
+# begin AAP specifics. -->
+# It is needed in desktop too, for generate-aap-metadata.
+target_link_libraries(frequalizer PUBLIC
+    juceaap_audio_plugin_client
+    )
+
+# begin Android specifics -->
+if (ANDROID)
+target_link_libraries(frequalizer PUBLIC
+    ${log}
+    ${android}
+    ${glesv2}
+    ${egl}
+    ${cpufeatures_lib}
+    ${oboe_lib}
+    )
+endif (ANDROID)
+# <-- end Android specifics
+# <-- end AAP specifics
+
+
 # setup the copying to the output folder
 if (APPLE)
     set(COPY_FOLDER ${CMAKE_SOURCE_DIR}/Builds/MacOSX)
@@ -53,7 +109,9 @@ elseif(WIN32)
     set(COPY_FOLDER ${CMAKE_SOURCE_DIR}/Builds/VisualStudio2015)
 endif()
 
+if (0)
 foreach(FORMAT ${FORMATS})
     get_target_property(ARTEFACTS_DIR frequalizer_${FORMAT} LIBRARY_OUTPUT_DIRECTORY)
     add_custom_command(TARGET frequalizer_${FORMAT} POST_BUILD COMMAND ${CMAKE_COMMAND} -E copy_directory ${ARTEFACTS_DIR} ${COPY_FOLDER})
-endforeach()
\ No newline at end of file
+endforeach()
+endif ()
\ No newline at end of file
diff --git a/External/JUCE b/External/JUCE
--- a/External/JUCE
+++ b/External/JUCE
@@ -1 +1 @@
-Subproject commit e6ec1819ec0a59a7cfe82d8ce72367f64e0a4bb0
+Subproject commit e6ec1819ec0a59a7cfe82d8ce72367f64e0a4bb0-dirty
